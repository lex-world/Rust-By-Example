 /* on Line: 10
 * @dev println is the macro which looks like the function
 * but ends with a bang (!)
 */

 /// @dev this is the main function that gets executed automatically during the build
fn main() {
    println!("Hello, world!");
}
